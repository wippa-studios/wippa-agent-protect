{
  "name": "@wippa/core",
  "version": "0.1.0",
  "description": "Sandboxed AI agent execution: clone, scan, build and run untrusted AI agent repositories in an isolated Docker container with egress filtering and secret masking.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "wippa": "dist/cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src/enterprise",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "ai",
    "agent",
    "sandbox",
    "docker",
    "security",
    "gvisor",
    "llm"
  ],
  "dependencies": {
    "execa": "^9.5.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.8.0"
  }
}